import React from "react";
import { ContactView } from "@/components/views/contact-view";

export const metadata = {
  title: "Contact Buildify — Start Your Project",
  description: "Connect with Buildify studio directly on WhatsApp or social channels.",
};

export default function ContactPage() {
  return <ContactView />;
}
