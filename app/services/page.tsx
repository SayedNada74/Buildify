import React from "react";
import { ServicesView } from "@/components/views/services-view";

export const metadata = {
  title: "Services & Capabilities — Buildify",
  description: "Focused digital experiences engineered around business growth and performance.",
};

export default function ServicesPage() {
  return <ServicesView />;
}
