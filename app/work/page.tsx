import React from "react";
import { WorkView } from "@/components/views/work-view";

export const metadata = {
  title: "Selected Work — Buildify",
  description: "A showcase of custom digital experiences engineered for performance and business results.",
};

export default function WorkPage() {
  return <WorkView />;
}
