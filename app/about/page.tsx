import React from "react";
import { AboutView } from "@/components/views/about-view";

export const metadata = {
  title: "About Buildify — Modern Digital Studio",
  description: "Learn about the philosophy, engineering mindset, and team behind Buildify.",
};

export default function AboutPage() {
  return <AboutView />;
}
