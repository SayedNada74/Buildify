import React from "react";

/**
 * High-performance lightweight overlay.
 * Replaced heavy SVG feTurbulence filter with zero-overhead null to ensure 60-144 FPS scrolling.
 */
export function NoiseOverlay() {
  return null;
}
